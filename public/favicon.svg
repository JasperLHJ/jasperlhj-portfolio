<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <defs>
    <radialGradient id="bg" cx="50%" cy="50%" r="60%">
      <stop offset="0%"   stop-color="#222018"/>
      <stop offset="100%" stop-color="#0e0d0b"/>
    </radialGradient>
  </defs>

  <!-- Background -->
  <circle cx="32" cy="32" r="32" fill="url(#bg)"/>

  <!-- Subtle border ring -->
  <circle cx="32" cy="32" r="30.5" fill="none" stroke="#c4bfb8" stroke-width="0.5" stroke-opacity="0.25"/>

  <!-- JL initials -->
  <text
    x="32" y="38"
    text-anchor="middle"
    font-family="'Cormorant Garamond', Georgia, serif"
    font-weight="300"
    font-size="22"
    letter-spacing="3"
    fill="#f7f4ef"
    fill-opacity="0.92"
  >JL</text>
</svg>
